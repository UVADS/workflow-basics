# Prefect requirements
# Install with: pip install -r requirements.txt

prefect>=2.0.0
numpy>=1.20.0
prefect-shell>=0.2.0

# Optional: Add specific integrations as needed
# prefect-aws
# prefect-azure
# prefect-gcp
# prefect-dask
# prefect-ray
# prefect-sqlalchemy

